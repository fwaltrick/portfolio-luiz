// src/types/index.ts
export interface Project {
  id: string
  title: string // Valor já traduzido
  titleKey: string // Chave para tradução
  category: string // Valor já traduzido
  categoryKey: string // Chave para tradução
  imageUrl: string
  slug: string
}
